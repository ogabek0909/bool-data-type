/*
    Create funcrtion func
    Check the logic "The variable "a" is negative"
    Args:
        a: int
    Returns:
        bool
    */
bool func(a) {
  return a < 0;
}

void main() {
  print(func(686));
}
