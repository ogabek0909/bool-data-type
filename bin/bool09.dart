/*
    Create function func
    Check the natural number.Natural numbers are numbers used in counting.
    Args:
        a: int
    Returns:
        bool
    */
bool func(a) {
  return a > 0;
}

void main() {
  print(func(-45));
}
